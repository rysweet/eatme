# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Student Artifact Package Share Evidence Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/student-artifact-package-share-evidence.yaml. It keeps the scenario at the editable asset and agentic evidence boundary so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
- name: instructor-acceptance-agent
  type: agentic
  config:
    timeout: 900000
    persona_asset: assets/personas/alice-user-crew.yaml
    scenario_asset: assets/scenarios/eatme/student-artifact-package-share-evidence.yaml
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-student-artifact-package-share-evidence}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"student-artifact-package-share-evidence"'
  timeout: 60000
- name: Run instructor agentic acceptance review
  agent: instructor-acceptance-agent
  action: agentic_test
  params:
    acceptance_probes: |-
      Artifact review packet checklist includes artifact or screenshot reference, student change, visible run result, attribution or classroom context, and one next revision.
      Student evidence handoff prompt asks the student to explain one Alice change and the visible behavior after running.
      Instructor review boundary note separates environment evidence from student learning evidence and names what still needs human judgment.
      Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, not complete Alice coverage, not hosted sharing, not deployed sharing, and not platform success.
    asset: assets/scenarios/eatme/student-artifact-package-share-evidence.yaml
    prompt: Act as an instructor acceptance agent preparing one student Alice artifact for classroom review handoff. Produce an artifact review packet checklist, student evidence handoff prompt, and instructor review boundary note grounded in the listed resources and this scenario. Require the packet to include the artifact or screenshot reference, what the student changed in Alice, the visible run result, attribution or classroom context, and one student-owned next revision. State explicitly that this is not full user interface automation, not automated creative assessment, not learner-world grading, not complete Alice coverage, not hosted sharing, not deployed sharing, and not platform success.
  expect:
    output_contains:
    - artifact_review_packet_checklist
    - student_evidence_handoff_prompt
    - instructor_review_boundary_note
  timeout: 900000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: Instructor Agentic Acceptance Review Covers Probes
  type: agentic_acceptance
  agent: instructor-acceptance-agent
  params:
    asset: assets/scenarios/eatme/student-artifact-package-share-evidence.yaml
    step: Run instructor agentic acceptance review
metadata:
  source_eatme_asset: assets/scenarios/eatme/student-artifact-package-share-evidence.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - student-artifact-package-share-evidence
  - student-artifact-package-share-evidence
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
