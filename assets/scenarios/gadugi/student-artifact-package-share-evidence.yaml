# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Student Artifact Package Share Evidence Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/student-artifact-package-share-evidence.yaml. It validates the editable scenario contract and runnable evidence steps so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-student-artifact-package-share-evidence}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/student-artifact-package-share-evidence.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "student-artifact-package-share-evidence"'
  timeout: 60000
- name: Validate instructor acceptance review contract
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-student-artifact-package-share-evidence}"
      cargo run -q -p eatme-cli -- assets validate --path 'assets/scenarios/eatme/student-artifact-package-share-evidence.yaml' --json
      SOURCE_ASSET='assets/scenarios/eatme/student-artifact-package-share-evidence.yaml'
      grep -F -- 'artifact_share_packet_checklist' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'student_evidence_handoff_prompt' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'instructor_review_boundary_note' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass validation uses ALICE_WEB_URL and /api/ responses when the web workflow is supported.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Artifact share packet checklist includes artifact or screenshot reference, student change, visible run result, attribution or classroom context, and one next revision.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student evidence handoff prompt asks the student to explain one Alice change and the visible behavior after running.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Instructor review boundary note separates environment evidence from student learning evidence and names what still needs human judgment.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, not complete Alice coverage, and not a deployed service.' "$SOURCE_ASSET" >/dev/null
      printf '%s\n' 'artifact_share_packet_checklist'
      printf '%s\n' 'student_evidence_handoff_prompt'
      printf '%s\n' 'instructor_review_boundary_note'
      printf '%s\n' instructor-acceptance-contract-ok
  expect:
    exit_code: 0
    stdout_contains:
    - artifact_share_packet_checklist
    - student_evidence_handoff_prompt
    - instructor_review_boundary_note
    - instructor-acceptance-contract-ok
  timeout: 60000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: Instructor Acceptance Review Contract Is Runnable
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate instructor acceptance review contract
metadata:
  source_eatme_asset: assets/scenarios/eatme/student-artifact-package-share-evidence.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - student-artifact-package-share-evidence
  - student-artifact-package-share-evidence
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
