# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Modified Class Portability Across Alice Projects
description: Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/modified-class-portability.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only.
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-modified-class-portability}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Launch Portability Smoke
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-modified-class-portability}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice run-howto --alice-home ${ALICE_HOME} --run-id ${RUN_ID} --runs-dir runs --timeout 900 --json --no-memory --offline-package --scenario modified-class-portability
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "modified-class-portability"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"startup_window_or_screenshot": {'
    - '"passed": true'
  timeout: 900000
- name: Verify Portability Evidence
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-modified-class-portability}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/modified-class-portability.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "modified-class-portability"'
  timeout: 60000
- name: Verify Lookingglass Class Behavior Package
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-modified-class-portability}"
      cd ${LOOKINGGLASS_HOME:?} && npm test -- test/entity-type-registry.test.ts src/project-io/class-behavior-package.persistence.test.ts src/server/routes/project-routes.class-behavior.test.ts
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: launch-portability-smoke succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Launch Portability Smoke
- name: verify-portability-evidence succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Verify Portability Evidence
- name: verify-lookingglass-class-behavior-package succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Verify Lookingglass Class Behavior Package
metadata:
  source_eatme_asset: assets/scenarios/eatme/modified-class-portability.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - modified-class-portability
  priority: critical
  author: eatme
  test_type: startup-check
