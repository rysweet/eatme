# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Starter Project Open/Save/Export Preflight
description: 'Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/starter-project-open-save-export-preflight.yaml. Alice desktop launch behavior remains owned by eatme; gadugi invokes eatme commands, records bounded starter-world and readiness-gap artifacts, and checks eatme launch-smoke evidence without claiming save/reopen/export coverage. This automation scenario keeps honest limits: opened starter project with manifest/log/window/screenshot evidence and bounded starter-world and readiness-gap artifacts only. User-facing Run-window state is not yet shown for the automation scenario observe step. User-facing observe-state evidence is not yet shown for the selected scenario and run. The adapter is not full UI automation, not full world execution, not creative assessment, not learner-world grading, not complete Alice coverage, not visible rendering correctness evidence, not deployed sharing/platform success, not first-lesson completion, and not full Save completion.'
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-starter-project-open-save-export-preflight}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 93'
  timeout: 60000
- name: Check Dependencies
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-starter-project-open-save-export-preflight}"
      cargo run -q -p eatme-cli -- deps check --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"all_required_available": true'
  timeout: 60000
- name: Record Starter World Change
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-starter-project-open-save-export-preflight}"
      bash -lc 'run_dir="runs/starter-project-open-save-export-preflight/${RUN_ID}"; mkdir -p "$run_dir"; printf "%s\n" "starter_world_change=In the next user-like Alice pass, add or rename one visible object in the bundled starter world and record what changed." "why=This keeps the starter-world build/change checkpoint editable and reviewable without grading the student project." "claim_boundary=This note is not creative assessment, not learner-world grading, and not evidence of a completed save." > "$run_dir/starter-world-change-note.txt"; printf "%s\n" "wrote=$run_dir/starter-world-change-note.txt"'
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
- name: Launch Smoke
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-starter-project-open-save-export-preflight}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice launch-smoke --alice-home ${ALICE_HOME} --run-id ${RUN_ID} --runs-dir runs --timeout 900 --json --no-memory --offline-package --scenario starter-project-open-save-export-preflight
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "starter-project-open-save-export-preflight"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"startup_window_or_screenshot": {'
    - africa.a3p
    - '"passed": true'
  timeout: 900000
- name: Record Run Observe Readiness Gaps
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-starter-project-open-save-export-preflight}"
      bash -lc 'run_dir="runs/starter-project-open-save-export-preflight/${RUN_ID}"; mkdir -p "$run_dir"; printf "%s\n" "run_or_observe_attempt=Use the eatme launch-smoke JSON report, log, and screenshot/window evidence to show that the opened starter world was run or observed as far as this automation scenario can document." "run_window_state=User-facing Run-window state is not yet shown for the automation scenario observe step." "observe_state_evidence=User-facing observe-state evidence is not yet shown for the selected scenario and run." "save_reopen_export_gaps=Record that save, reopen, export, and classroom readiness still need user-like evidence before those journeys are trusted." "claim_boundary=not full UI automation, not full world execution, not visible rendering correctness evidence, not deployed sharing/platform success, not first-lesson completion, not grading, not creative assessment, and not full Save completion." > "$run_dir/run-observe-readiness-gaps.txt"; printf "%s\n" "launch=Use the eatme launch-smoke JSON report, log, and screenshot/window evidence for the opened starter project." "starter_world_change=Review starter-world-change-note.txt for the small editable starter-world change to make in a later user-like Alice pass." "run_or_observe_attempt=Treat the available log and screenshot/window evidence as an attempted run or observation only." "run_window_state=User-facing Run-window state is not yet shown for the automation scenario observe step." "observe_state_evidence=User-facing observe-state evidence is not yet shown for the selected scenario and run." "save_reopen_export_readiness_gaps=Save, reopen, export, and classroom readiness still need user-like evidence before those journeys are trusted." "claim_boundary=not full UI automation, not full world execution, not visible rendering correctness evidence, not deployed sharing/platform success, not first-lesson completion, not grading, not creative assessment, and not full Save completion." > "$run_dir/starter-project-readiness-report.txt"; printf "%s\n" "wrote=$run_dir/run-observe-readiness-gaps.txt" "wrote=$run_dir/starter-project-readiness-report.txt"'
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: check-dependencies succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Check Dependencies
- name: record-starter-world-change succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Starter World Change
- name: launch-smoke succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Launch Smoke
- name: record-run-observe-readiness-gaps succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Run Observe Readiness Gaps
metadata:
  source_eatme_asset: assets/scenarios/eatme/starter-project-open-save-export-preflight.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - starter-project-open-save-export-preflight
  priority: critical
  author: eatme
  test_type: launch-smoke
