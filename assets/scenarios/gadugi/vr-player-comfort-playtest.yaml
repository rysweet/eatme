# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme VR Player Comfort Playtest
description: Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/vr-player-comfort-playtest.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only.
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-vr-player-comfort-playtest}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Record Vr Player Preflight
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-vr-player-comfort-playtest}"
      bash -lc 'set -e; run_dir="runs/vr-player-comfort-playtest/${RUN_ID}"; mkdir -p "$run_dir"; if [ "${EATME_REAL_VR:-0}" = "1" ] && [ "${VR_HEADSET_AVAILABLE:-0}" = "1" ]; then printf "%s\n" "real_vr_available=true" "required_evidence=headset-or-player-capture,comfort-notes,observer-log,revision-note" > "$run_dir/vr-player-preflight.txt"; else printf "%s\n" "real_vr_available=false" "required_evidence=desktop-camera-view,orientation-note,comfort-check,fallback-playtest-sheet" > "$run_dir/vr-player-preflight.txt"; fi; test -s "$run_dir/vr-player-preflight.txt"; grep -Eq "real_vr_available=(true|false)" "$run_dir/vr-player-preflight.txt"; printf "%s\n" "wrote=$run_dir/vr-player-preflight.txt"'
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
- name: Record Comfort Playtest Guidance Template
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-vr-player-comfort-playtest}"
      bash -lc 'set -e; run_dir="runs/vr-player-comfort-playtest/${RUN_ID}"; template="$run_dir/comfort-playtest-guidance-template.md"; mkdir -p "$run_dir"; printf "%s\n" "# Comfort playtest guidance template" "" "This is guidance only; it does not validate that a playtest observation has occurred." "" "- VR available: see vr-player-preflight.txt." "- Comfort check: record observed comfort, stop points, and motion concerns." "- Orientation note: record observed whether the player knows where to look and go." "- Player cue: record observed what helped or confused the player." "- Fallback path: record observed desktop camera or screen viewer notes when no headset is available." "- Revision decision: record the change made from observed feedback or why no change is needed." > "$template"; test -s "$template"; grep -q "guidance only" "$template"; grep -q "Revision decision" "$template"; printf "%s\n" "wrote=$template"'
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
- name: Launch Smoke
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-vr-player-comfort-playtest}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice run-howto --alice-home ${ALICE_HOME} --run-id ${RUN_ID} --runs-dir runs --timeout 900 --json --no-memory --offline-package --scenario vr-player-comfort-playtest
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "vr-player-comfort-playtest"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"startup_window_or_screenshot": {'
    - '"passed": true'
  timeout: 900000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: record-vr-player-preflight succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Vr Player Preflight
- name: record-comfort-playtest-guidance-template succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Comfort Playtest Guidance Template
- name: launch-smoke succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Launch Smoke
metadata:
  source_eatme_asset: assets/scenarios/eatme/vr-player-comfort-playtest.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - vr-player-comfort-playtest
  priority: critical
  author: eatme
  test_type: startup-check
