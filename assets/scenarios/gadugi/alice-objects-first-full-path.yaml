# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Alice Objects-First Full Path
description: 'Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/alice-objects-first-full-path.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only. This adapter preserves the source boundary: not full UI automation, not creative assessment, and not learner-world grading.'
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-full-path}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Check Dependencies
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-full-path}"
      cargo run -q -p eatme-cli -- deps check --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"all_required_available": true'
  timeout: 60000
- name: Run Objects First Full Path
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-full-path}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice objects-first-full-path --alice-home "${ALICE_HOME:?}" --run-id "${RUN_ID:?}" --runs-dir runs --timeout 900 --json --no-memory --offline-package
  expect:
    exit_code: 1
    stdout_contains:
    - '"scenario_id": "alice-objects-first-full-path"'
    - '"failure_category":'
    - '"real_alice_execution_evidence": {'
    - '"specific_alice_window_detected": {'
    - '"activate_alice_window_ui_action": {'
    - '"place_object_ui_action": {'
    - '"edit_procedure_ui_action": {'
    - '"run_world_ui_action": {'
    - '"save_project_ui_action": {'
    - '"ui_action_artifact_captured": {'
    - '"passed": true'
  timeout: 900000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: check-dependencies succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Check Dependencies
- name: run-objects-first-full-path expected failure is explicit
  type: output_contains_all
  agent: eatme-cli-agent
  params:
    required_strings: |-
      "scenario_id": "alice-objects-first-full-path"
      "failure_category":
      "real_alice_execution_evidence": {
      "specific_alice_window_detected": {
      "activate_alice_window_ui_action": {
      "place_object_ui_action": {
      "edit_procedure_ui_action": {
      "run_world_ui_action": {
      "save_project_ui_action": {
      "ui_action_artifact_captured": {
      "passed": true
    step: Run Objects First Full Path
metadata:
  source_eatme_asset: assets/scenarios/eatme/alice-objects-first-full-path.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - alice-objects-first-full-path
  priority: critical
  author: eatme
  test_type: ui-action-contract
