# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Alice Objects-First World
description: Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/alice-objects-first-world.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only.
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Check Dependencies
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      cargo run -q -p eatme-cli -- deps check --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"all_required_available": true'
  timeout: 60000
- name: Create Or Open Project
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice run-objects-first-world --alice-home "${ALICE_HOME:?}" --run-id "${RUN_ID:?}" --runs-dir runs --timeout 900 --json --no-memory --offline-package
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "alice-objects-first-world"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"passed": true'
  timeout: 60000
- name: Add Visible Object
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/object-placement/placement.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/object-placement/placement.json
    - passed=true
  timeout: 60000
- name: Position And Transform Object
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/object-transform/object-transform.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/object-transform/object-transform.json
    - passed=true
  timeout: 60000
- name: Edit Movement Procedure
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/procedure-edit/procedure-movement.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/procedure-edit/procedure-movement.json
    - passed=true
  timeout: 60000
- name: Run World
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/world-run/world-run.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/world-run/world-run.json
    - passed=true
  timeout: 60000
- name: Save Project
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/project-save/project-save.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/project-save/project-save.json
    - passed=true
  timeout: 60000
- name: Reopen Project
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/project-reopen/project-reopen.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/project-reopen/project-reopen.json
    - passed=true
  timeout: 60000
- name: Verify Persisted State
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/project-reopen/persisted-state.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/project-reopen/persisted-state.json
    - passed=true
  timeout: 60000
- name: Record Evidence
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-objects-first-world}"
      inspect runs/alice-objects-first-world/${RUN_ID}/objects-first-evidence.json
  expect:
    exit_code: 0
    stdout_contains:
    - runs/alice-objects-first-world/${RUN_ID}/objects-first-evidence.json
    - passed=true
  timeout: 60000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: check-dependencies succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Check Dependencies
- name: create-or-open-project succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Create Or Open Project
- name: add-visible-object succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Add Visible Object
- name: position-and-transform-object succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Position And Transform Object
- name: edit-movement-procedure succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Edit Movement Procedure
- name: run-world succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Run World
- name: save-project succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Save Project
- name: reopen-project succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Reopen Project
- name: verify-persisted-state succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Verify Persisted State
- name: record-evidence succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Evidence
metadata:
  source_eatme_asset: assets/scenarios/eatme/alice-objects-first-world.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - alice-objects-first-world
  priority: critical
  author: eatme
  test_type: objects-first-workflow
