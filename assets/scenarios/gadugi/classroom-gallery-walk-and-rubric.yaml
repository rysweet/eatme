# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Classroom Gallery Walk And Rubric Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/classroom-gallery-walk-and-rubric.yaml. It validates the editable scenario contract and runnable evidence steps so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - RUN_ID
  - EATME_REPO
  - ALICE_WEB_URL
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-classroom-gallery-walk-and-rubric}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/classroom-gallery-walk-and-rubric.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "classroom-gallery-walk-and-rubric"'
  timeout: 60000
- name: Lookingglass Gallery Rubric Evidence
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-classroom-gallery-walk-and-rubric}"
      set -e
      EATME_WEB_PLATFORM=1 ALICE_WEB_URL=${ALICE_WEB_URL:-http://localhost:3099} cargo test -p eatme-alice --test web_platform_curriculum_e2e -- live_classroom_gallery_walk_exercises_rubric_api --test-threads=1
      printf "%s\n" "cargo-test-ok=web_platform_curriculum_e2e"
  expect:
    exit_code: 0
    stdout_contains:
    - cargo-test-ok=web_platform_curriculum_e2e
    - 'test result: ok'
  timeout: 60000
- name: Validate instructor acceptance review contract
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-classroom-gallery-walk-and-rubric}"
      cargo run -q -p eatme-cli -- assets validate --path 'assets/scenarios/eatme/classroom-gallery-walk-and-rubric.yaml' --json
      SOURCE_ASSET='assets/scenarios/eatme/classroom-gallery-walk-and-rubric.yaml'
      grep -F -- 'gallery_walk_rubric' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'peer_feedback_card' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'creator_response_prompt' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'revision_checkpoint_note' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'instructor_boundary_note' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'lookingglass_gallery_rubric_api_evidence' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass validation uses ALICE_WEB_URL and /api/ responses when the web workflow is supported.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass /api/review/gallery-walk-rubric returns visible gallery items, review prompts, rubric criteria, review recording evidence, and live studio support evidence.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Gallery walk rubric works for stories, games, simulations, media scenes, or starter projects without enforcing one project template.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Peer feedback card asks for observed artifact behavior, concept or design connection, respectful question, and suggested revision.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Creator response prompt asks the student to compare peer observation with their own intent and name one student-owned next step.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Revision checkpoint note ties peer feedback to a visible behavior, debugging/process evidence, reflection, or accessibility/audience improvement.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output connects assessment-curator, studio-facilitator, collaborative-peer-mentor, reflective-debugger, and accessibility-advocate personas to reviewable classroom evidence.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, not complete Alice coverage, and not a deployed sharing service.' "$SOURCE_ASSET" >/dev/null
      printf '%s\n' 'gallery_walk_rubric'
      printf '%s\n' 'peer_feedback_card'
      printf '%s\n' 'creator_response_prompt'
      printf '%s\n' 'revision_checkpoint_note'
      printf '%s\n' 'instructor_boundary_note'
      printf '%s\n' 'lookingglass_gallery_rubric_api_evidence'
      printf '%s\n' instructor-acceptance-contract-ok
  expect:
    exit_code: 0
    stdout_contains:
    - gallery_walk_rubric
    - peer_feedback_card
    - creator_response_prompt
    - revision_checkpoint_note
    - instructor_boundary_note
    - lookingglass_gallery_rubric_api_evidence
    - instructor-acceptance-contract-ok
  timeout: 60000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: lookingglass-gallery-rubric-evidence succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Lookingglass Gallery Rubric Evidence
- name: Instructor Acceptance Review Contract Is Runnable
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate instructor acceptance review contract
metadata:
  source_eatme_asset: assets/scenarios/eatme/classroom-gallery-walk-and-rubric.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - classroom-gallery-walk-and-rubric
  - classroom-gallery-walk-and-rubric
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
