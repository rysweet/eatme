# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Audio, Camera, and Export — Sharecase Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/audio-camera-and-export-sharecase.yaml. It validates the editable scenario contract and runnable evidence steps so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-audio-camera-and-export-sharecase}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/audio-camera-and-export-sharecase.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "audio-camera-and-export-sharecase"'
  timeout: 60000
- name: Validate instructor acceptance review contract
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-audio-camera-and-export-sharecase}"
      cargo run -q -p eatme-cli -- assets validate --path 'assets/scenarios/eatme/audio-camera-and-export-sharecase.yaml' --json
      SOURCE_ASSET='assets/scenarios/eatme/audio-camera-and-export-sharecase.yaml'
      grep -F -- 'camera_and_audio_polish_guide' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'student_sharecase_card' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'accessibility_aware_sharing_plan' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass validation uses ALICE_WEB_URL and /api/ responses when the web workflow is supported.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass export/share evidence is limited to LookingGlass:test/project-export-share-fallback.contract.test.ts and does not claim native Web Share success.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass audio evidence remains bounded metadata/playback-bridge evidence and does not claim native audio playback.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Camera-and-audio polish guide prompts the student to choose one camera marker or audio cue and write one sentence explaining why they chose it.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Camera-and-audio polish guide includes a caption or text-label step that makes the cue meaningful for viewers who cannot rely solely on sound or visual detail.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student sharecase card names the artifact or screenshot reference and describes the one cue the student added.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student sharecase card asks one targeted peer-feedback question that addresses concept or communication rather than only aesthetics.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Accessibility-aware sharing plan lists at least two delivery alternatives in case export fails.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Accessibility-aware sharing plan includes one accessibility-aware communication choice described as a real design decision, not an optional add-on.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output explains that peer feedback must target concept and communication, not only aesthetics, and states why.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output notes that export format, LMS, and media pipeline choices vary by classroom policy.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, not complete Alice coverage, and not a real Alice startup check.' "$SOURCE_ASSET" >/dev/null
      printf '%s\n' 'camera_and_audio_polish_guide'
      printf '%s\n' 'student_sharecase_card'
      printf '%s\n' 'accessibility_aware_sharing_plan'
      printf '%s\n' instructor-acceptance-contract-ok
  expect:
    exit_code: 0
    stdout_contains:
    - camera_and_audio_polish_guide
    - student_sharecase_card
    - accessibility_aware_sharing_plan
    - instructor-acceptance-contract-ok
  timeout: 60000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: Instructor Acceptance Review Contract Is Runnable
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate instructor acceptance review contract
metadata:
  source_eatme_asset: assets/scenarios/eatme/audio-camera-and-export-sharecase.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - audio-camera-and-export-sharecase
  - audio-camera-and-export-sharecase
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
