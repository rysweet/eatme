# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Alice Web Story API Runtime Parity
description: Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/alice-web-story-api-runtime-parity.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only.
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-story-api-runtime-parity}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Record Runtime Parity Gap Matrix
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-story-api-runtime-parity}"
      bash -lc 'run_dir="runs/alice-web-story-api-runtime-parity/${RUN_ID}"; mkdir -p "$run_dir"; cat > "$run_dir/story-api-runtime-parity-gaps.md" <<EOF # Story API Runtime Parity Gaps | gap | Java Alice baseline | Web/eatme probe | closure test | | --- | --- | --- | --- | | procedures and parameters | argument binding drives visible method behavior | parameter and function grading distinguish correct and incorrect usage | cargo test -p eatme-alice --test parameters_e2e --test functions_e2e | | loops and conditionals | repeated and conditional actions affect the world predictably | nested control flow and comparison scenarios pass | cargo test -p eatme-alice --test loops_and_conditionals_e2e --test nested_control_flow_e2e | | events/collision/proximity | listeners fire from real interaction and state changes | listener, collision, and proximity tests verify callbacks and outcomes | cargo test -p eatme-alice --test events_collision_support --test events_and_collision_e2e | | text and speech | visible speech/text behavior is real or explicitly bounded | text/speech tests identify real output versus simulation | cargo test -p eatme-alice --test text_and_speech_e2e | EOF printf "%s\n" "wrote=$run_dir/story-api-runtime-parity-gaps.md"'
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
- name: Run Runtime Closure Probes
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-story-api-runtime-parity}"
      bash -lc 'cargo test -p eatme-alice --test parameters_e2e && cargo test -p eatme-alice --test functions_e2e && cargo test -p eatme-alice --test loops_and_conditionals_e2e && cargo test -p eatme-alice --test nested_control_flow_e2e && cargo test -p eatme-alice --test events_collision_support && cargo test -p eatme-alice --test events_and_collision_e2e && cargo test -p eatme-alice --test text_and_speech_e2e'
  expect:
    exit_code: 0
    stdout_contains: []
  timeout: 60000
- name: Launch Smoke
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-story-api-runtime-parity}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice launch-smoke --alice-home "${ALICE_HOME:?}" --run-id "${RUN_ID:?}" --runs-dir runs --timeout 900 --json --no-memory --offline-package --scenario alice-web-story-api-runtime-parity
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "alice-web-story-api-runtime-parity"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"startup_window_or_screenshot": {'
    - '"passed": true'
  timeout: 900000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: record-runtime-parity-gap-matrix succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Runtime Parity Gap Matrix
- name: run-runtime-closure-probes succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Run Runtime Closure Probes
- name: launch-smoke succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Launch Smoke
metadata:
  source_eatme_asset: assets/scenarios/eatme/alice-web-story-api-runtime-parity.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - alice-web-story-api-runtime-parity
  priority: critical
  author: eatme
  test_type: startup-check
