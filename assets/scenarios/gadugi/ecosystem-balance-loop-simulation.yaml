# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Ecosystem Balance Loop Simulation Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/ecosystem-balance-loop-simulation.yaml. It validates the editable scenario contract and runnable evidence steps so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-ecosystem-balance-loop-simulation}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/ecosystem-balance-loop-simulation.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "ecosystem-balance-loop-simulation"'
  timeout: 60000
- name: Validate instructor acceptance review contract
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-ecosystem-balance-loop-simulation}"
      cargo run -q -p eatme-cli -- assets validate --path 'assets/scenarios/eatme/ecosystem-balance-loop-simulation.yaml' --json
      SOURCE_ASSET='assets/scenarios/eatme/ecosystem-balance-loop-simulation.yaml'
      grep -F -- 'lesson_brief' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'student_activity_sheet' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'loop_round_frame' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass validation uses ALICE_WEB_URL and /api/ responses when the web workflow is supported.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Lesson brief shows duplicated per-round animal calls before introducing the loop.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Loop-round frame names the loop counter and round body without referencing specific Alice assets.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student activity sheet prompts a written prediction of scene state after N rounds before any test run.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student activity sheet prompts recording of observed visible behavior after the run.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output connects systems-puzzle-solver and playful-tinkerer student personas to visible classroom evidence.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, and not complete Alice coverage.' "$SOURCE_ASSET" >/dev/null
      printf '%s\n' 'lesson_brief'
      printf '%s\n' 'student_activity_sheet'
      printf '%s\n' 'loop_round_frame'
      printf '%s\n' instructor-acceptance-contract-ok
  expect:
    exit_code: 0
    stdout_contains:
    - lesson_brief
    - student_activity_sheet
    - loop_round_frame
    - instructor-acceptance-contract-ok
  timeout: 60000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: Instructor Acceptance Review Contract Is Runnable
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate instructor acceptance review contract
metadata:
  source_eatme_asset: assets/scenarios/eatme/ecosystem-balance-loop-simulation.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - ecosystem-balance-loop-simulation
  - ecosystem-balance-loop-simulation
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
