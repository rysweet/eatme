# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Alice Web Gallery and Media Parity
description: Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/alice-web-gallery-media-parity.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only.
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-gallery-media-parity}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Record Gallery Media Gap Matrix
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-gallery-media-parity}"
      bash -lc 'run_dir="runs/alice-web-gallery-media-parity/${RUN_ID}"; mkdir -p "$run_dir"; cat > "$run_dir/gallery-media-parity-gaps.md" <<EOF # Gallery and Media Parity Gaps | gap | Java Alice baseline | Web/eatme probe | closure test | | --- | --- | --- | --- | | starter gallery coverage | starter projects expose scene entities, resources, joints, cameras, audio, and billboards | gallery/content coverage scans real or synthetic archives | cargo test -p eatme-alice --test a3p_gallery_coverage --test a3p_content_coverage | | camera and viewpoint | camera transforms and viewpoints are preserved and user-visible | camera/viewpoint scenarios assert observable evidence | cargo test -p eatme-alice --test camera_and_viewpoint_e2e | | media and speech | audio/text assets are present or explicitly bounded | media/text tests record real output versus bounded simulation | cargo test -p eatme-alice --test text_and_speech_e2e --test import_export_support | | model import/export | imported model has provenance, texture visibility, scale/orientation, and fallback evidence | import/export tests keep runnable-world evidence explicit | cargo test -p eatme-alice --test import_export_workflow_real --test project_io_resource_management | EOF printf "%s\n" "wrote=$run_dir/gallery-media-parity-gaps.md"'
  expect:
    exit_code: 0
    stdout_contains:
    - a3p_gallery_coverage
    - a3p_content_coverage
    - camera_and_viewpoint_e2e
    - text_and_speech_e2e
    - import_export_support
    - import_export_workflow_real
    - project_io_resource_management
    - 'test result: ok'
  timeout: 60000
- name: Run Gallery Media Closure Probes
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-gallery-media-parity}"
      bash -lc 'cargo test -p eatme-alice --test a3p_content_coverage && cargo test -p eatme-alice --test camera_and_viewpoint_e2e && cargo test -p eatme-alice --test text_and_speech_e2e && cargo test -p eatme-alice --test import_export_support && cargo test -p eatme-alice --test project_io_resource_management'
  expect:
    exit_code: 0
    stdout_contains:
    - a3p_content_coverage
    - camera_and_viewpoint_e2e
    - text_and_speech_e2e
    - import_export_support
    - 'test result: ok'
  timeout: 60000
- name: Launch Smoke
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-gallery-media-parity}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice launch-smoke --alice-home "${ALICE_HOME:?}" --run-id "${RUN_ID:?}" --runs-dir runs --timeout 900 --json --no-memory --offline-package --scenario alice-web-gallery-media-parity
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "alice-web-gallery-media-parity"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"startup_window_or_screenshot": {'
    - '"passed": true'
  timeout: 900000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: record-gallery-media-gap-matrix succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record Gallery Media Gap Matrix
- name: run-gallery-media-closure-probes succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Run Gallery Media Closure Probes
- name: launch-smoke succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Launch Smoke
metadata:
  source_eatme_asset: assets/scenarios/eatme/alice-web-gallery-media-parity.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - alice-web-gallery-media-parity
  priority: critical
  author: eatme
  test_type: startup-check
