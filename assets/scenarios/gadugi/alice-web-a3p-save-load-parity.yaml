# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Alice Web A3P Save/Load Parity
description: Gadugi-compatible CLI scenario generated from assets/scenarios/eatme/alice-web-a3p-save-load-parity.yaml. Alice desktop behavior remains owned by eatme; gadugi invokes eatme commands and checks manifest-level evidence only.
version: 1.0.0
config:
  timeout: 1800000
  retries: 0
  parallel: false
environment:
  requires:
  - ALICE_HOME
  - EATME_REAL_ALICE
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 1800000
    capture_output: true
steps:
- name: Validate Assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-a3p-save-load-parity}"
      cargo run -q -p eatme-cli -- assets validate --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"scenario_asset_count": 115'
  timeout: 60000
- name: Record A3p Parity Gap Matrix
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-a3p-save-load-parity}"
      set -e
      bash -lc 'run_dir="runs/alice-web-a3p-save-load-parity/${RUN_ID}";
      mkdir -p "$run_dir";
      cat > "$run_dir/a3p-save-load-parity-gaps.md" <<EOF
      # A3P Save/Load Parity Gaps
      | gap | Java Alice baseline | Web/eatme probe | closure test |
      | --- | --- | --- | --- |
      | project identity | starter filename and project metadata remain user-visible | parsed generic Program names must not hide requested filename | cargo test -p eatme-alice --test real_a3p_pipeline_integration |
      | resources | model/texture/audio resources survive save and reopen | resource declarations and archive paths are preserved | cargo test -p eatme-alice --test a3p_content_coverage |
      | statements | procedure bodies preserve method calls, loops, conditionals, and events | parser/writer round-trip keeps supported statement kinds explicit | cargo test -p eatme-alice --test a3p_roundtrip_coverage |
      | archive safety | unsafe archive paths are rejected or ignored loudly | path traversal and oversized archive limits are enforced | cargo test -p eatme-alice --test malformed_input_resilience |
      EOF
      printf "%s\n" "wrote=$run_dir/a3p-save-load-parity-gaps.md"'
      printf "%s\n" "cargo-test-ok=real_a3p_pipeline_integration"
      printf "%s\n" "cargo-test-ok=a3p_content_coverage"
      printf "%s\n" "cargo-test-ok=a3p_roundtrip_coverage"
      printf "%s\n" "cargo-test-ok=malformed_input_resilience"
  expect:
    exit_code: 0
    stdout_contains:
    - wrote=
    - a3p-save-load-parity-gaps.md
  timeout: 60000
- name: Run A3p Closure Probes
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-a3p-save-load-parity}"
      set -e
      bash -lc 'cargo test -p eatme-alice --test a3p_content_coverage && cargo test -p eatme-alice --test a3p_roundtrip_coverage && cargo test -p eatme-alice --test real_a3p_pipeline_integration && cargo test -p eatme-alice --test malformed_input_resilience'
      printf "%s\n" "cargo-test-ok=a3p_content_coverage"
      printf "%s\n" "cargo-test-ok=a3p_roundtrip_coverage"
      printf "%s\n" "cargo-test-ok=real_a3p_pipeline_integration"
      printf "%s\n" "cargo-test-ok=malformed_input_resilience"
  expect:
    exit_code: 0
    stdout_contains:
    - cargo-test-ok=a3p_content_coverage
    - cargo-test-ok=a3p_roundtrip_coverage
    - cargo-test-ok=real_a3p_pipeline_integration
    - cargo-test-ok=malformed_input_resilience
    - 'test result: ok'
  timeout: 60000
- name: Launch Smoke
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-alice-web-a3p-save-load-parity}"
      EATME_REAL_ALICE=1 cargo run -q -p eatme-cli -- alice launch-smoke --alice-home "${ALICE_HOME:?}" --run-id "${RUN_ID:?}" --runs-dir runs --timeout 900 --json --no-memory --offline-package --scenario alice-web-a3p-save-load-parity
  expect:
    exit_code: 0
    stdout_contains:
    - '"scenario_id": "alice-web-a3p-save-load-parity"'
    - '"failure_category": null'
    - '"real_alice_execution_evidence": {'
    - '"startup_window_or_screenshot": {'
    - '"passed": true'
  timeout: 900000
assertions:
- name: validate-assets succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate Assets
- name: record-a3p-parity-gap-matrix succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Record A3p Parity Gap Matrix
- name: run-a3p-closure-probes succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Run A3p Closure Probes
- name: launch-smoke succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Launch Smoke
metadata:
  source_eatme_asset: assets/scenarios/eatme/alice-web-a3p-save-load-parity.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - alice-web-a3p-save-load-parity
  priority: critical
  author: eatme
  test_type: startup-check
