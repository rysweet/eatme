# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Accessibility Rescue — Camera and Captions Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/accessibility-rescue-camera-captions.yaml. It validates the editable scenario contract and runnable evidence steps so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - RUN_ID
  - EATME_REPO
  - ALICE_WEB_URL
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-accessibility-rescue-camera-captions}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/accessibility-rescue-camera-captions.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "accessibility-rescue-camera-captions"'
  timeout: 60000
- name: Lookingglass Caption Evidence
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-accessibility-rescue-camera-captions}"
      set -e
      EATME_WEB_PLATFORM=1 ALICE_WEB_URL=${ALICE_WEB_URL:-http://localhost:3099} cargo test -p eatme-alice --test web_platform_curriculum_e2e -- live_accessibility_rescue_camera_captions_exercises_caption_api --test-threads=1
      printf "%s\n" "cargo-test-ok=web_platform_curriculum_e2e"
  expect:
    exit_code: 0
    stdout_contains:
    - cargo-test-ok=web_platform_curriculum_e2e
    - 'test result: ok'
  timeout: 60000
- name: Validate instructor acceptance review contract
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-accessibility-rescue-camera-captions}"
      set -euo pipefail
      cargo run -q -p eatme-cli -- assets validate --path 'assets/scenarios/eatme/accessibility-rescue-camera-captions.yaml' --json
      AGENTIC_TEST_PROMPT='Act as an instructor acceptance agent preparing an accessibility rescue Alice lesson. Produce an accessibility audit card, a camera rescue guide, and a caption and label template grounded in the listed resources and this accessibility-rescue-camera-captions scenario. The accessibility audit card must prompt the student to review a starter scene and identify at least two accessibility gaps: at least one camera framing gap (such as an angle that hides key action from a viewer who cannot move closer) and at least one caption or label gap (such as spoken dialogue or an audio event with no visible text equivalent). The camera rescue guide must show one concrete example of reframing or adding a second camera position that makes obscured action visible, explain that any starter scene may replace the example, and state that the goal is to ensure a viewer who cannot rely on fine visual detail can follow the essential action. The caption and label template must prompt the student to add an on-screen text label for each identified caption gap, state what essential message the label conveys, and note that the label should match the pacing of the scene so it appears when the corresponding sound or action occurs. The reflection prompt must ask what each fix improved, which type of viewer benefits most from each fix, and what accessibility gap remains and why. Include a note that audio cues, high- contrast text, and simplified camera paths are all valid accessibility rescue strategies. Include an optional extension that shows how to identify a moment where an audio cue reinforces a visual event, and make clear that the extension does not block learners who complete only the two-fix version. State explicitly that this is not full user interface automation, not automated creative assessment, not learner-world grading, and not complete Alice coverage.'
      printf '%s\n' "$AGENTIC_TEST_PROMPT" >/dev/null
      SOURCE_ASSET='assets/scenarios/eatme/accessibility-rescue-camera-captions.yaml'
      grep -F -- 'accessibility_audit_card' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'camera_rescue_guide' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'caption_and_label_template' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'lookingglass_caption_api_evidence' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass validation uses ALICE_WEB_URL and /api/ responses when the web workflow is supported.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass /api/accessibility/rescue-camera-captions returns aria-live, camera, and scene-object caption checks for the web workflow.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Accessibility audit card prompts identification of at least one camera framing gap.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Accessibility audit card prompts identification of at least one caption or label gap.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Camera rescue guide shows a concrete camera reframe or second-position example before the general framing.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Caption and label template prompts an on-screen text label that states the essential message and pacing.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Reflection prompt asks which viewer benefits most from each fix and what gap remains.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output notes that audio cues, high-contrast text, and simplified camera paths are valid accessibility rescue strategies.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Optional audio cue extension is clearly optional and does not block learners who complete only the two-fix version.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, and not complete Alice coverage.' "$SOURCE_ASSET" >/dev/null
      printf '%s\n' 'accessibility_audit_card'
      printf '%s\n' 'camera_rescue_guide'
      printf '%s\n' 'caption_and_label_template'
      printf '%s\n' 'lookingglass_caption_api_evidence'
      printf '%s\n' instructor-acceptance-contract-ok
  expect:
    exit_code: 0
    stdout_contains:
    - accessibility_audit_card
    - camera_rescue_guide
    - caption_and_label_template
    - lookingglass_caption_api_evidence
    - instructor-acceptance-contract-ok
  timeout: 60000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: lookingglass-caption-evidence succeeds
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Lookingglass Caption Evidence
- name: Instructor Acceptance Review Contract Is Runnable
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate instructor acceptance review contract
metadata:
  source_eatme_asset: assets/scenarios/eatme/accessibility-rescue-camera-captions.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - accessibility-rescue-camera-captions
  - accessibility-rescue-camera-captions
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
