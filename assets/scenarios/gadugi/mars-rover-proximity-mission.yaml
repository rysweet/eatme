# DO NOT EDIT: generated by eatme-assets from canonical eatme scenario assets.
# Non-coders: edit the matching source YAML under assets/scenarios/eatme/, then ask a maintainer to run `eatme assets generate-gadugi`.
name: Eatme Mars Rover Proximity Mission Agentic Flow
description: Gadugi-compatible instructor acceptance adapter generated from assets/scenarios/eatme/mars-rover-proximity-mission.yaml. It validates the editable scenario contract and runnable evidence steps so non-coders can maintain prompts, probes, and rubrics without changing Rust.
version: 1.0.0
config:
  timeout: 900000
  retries: 0
  parallel: false
environment:
  requires: []
  optional:
  - RUN_ID
  - EATME_REPO
agents:
- name: eatme-cli-agent
  type: system
  config:
    shell: bash
    cwd: .
    timeout: 60000
    capture_output: true
steps:
- name: Validate editable Alice instructor assets
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-mars-rover-proximity-mission}"
      cargo run -q -p eatme-cli -- assets validate --path assets/scenarios/eatme/mars-rover-proximity-mission.yaml --json
  expect:
    exit_code: 0
    stdout_contains:
    - '"passed": true'
    - '"id": "mars-rover-proximity-mission"'
  timeout: 60000
- name: Validate instructor acceptance review contract
  agent: eatme-cli-agent
  action: execute_command
  params:
    command: |-
      cd "${EATME_REPO:-.}"
      export RUN_ID="${RUN_ID:-gadugi-mars-rover-proximity-mission}"
      set -euo pipefail
      cargo run -q -p eatme-cli -- assets validate --path 'assets/scenarios/eatme/mars-rover-proximity-mission.yaml' --json
      AGENTIC_TEST_PROMPT='Act as an instructor acceptance agent preparing a Mars rover proximity mission Alice lesson. Produce a lesson brief, student activity sheet, and proximity-range frame grounded in the listed resources and this mars-rover-proximity-mission scenario. The lesson brief must show a short motivating example of a rover moving toward a rock hazard with no event handler first, explain what changes when a proximity event handler is added, and introduce a proximity-range frame the student sets before running. The student activity sheet must prompt: place a rover and at least one rock hazard in the scene (without naming specific Alice character files), write a proximity event handler that triggers an avoidance action when the rover is within a chosen range, choose a range value and write a prediction of whether the rover will react before or after reaching the hazard, run the world, and record what visible behavior changed compared to the prediction. The proximity-range frame must name the proximity threshold, the event name, and the avoidance action without requiring specific Alice assets or character models. State explicitly that this is not full user interface automation, not automated creative assessment, not learner-world grading, and not complete Alice coverage.'
      printf '%s\n' "$AGENTIC_TEST_PROMPT" >/dev/null
      SOURCE_ASSET='assets/scenarios/eatme/mars-rover-proximity-mission.yaml'
      grep -F -- 'lesson_brief' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'student_activity_sheet' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'proximity_range_frame' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'LookingGlass validation uses ALICE_WEB_URL and /api/ responses when the web workflow is supported.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Lesson brief shows the rover moving with no proximity handler before introducing the event-driven version.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Proximity-range frame names the proximity threshold and avoidance action without referencing specific Alice assets.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student activity sheet prompts a written prediction of when the reaction fires before any test run.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Student activity sheet includes an observation log comparing prediction to visible outcome after running.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output connects systems-puzzle-solver and playful-tinkerer student personas to visible classroom evidence.' "$SOURCE_ASSET" >/dev/null
      grep -F -- 'Output states the scenario is not full user interface automation, not automated creative assessment, not learner-world grading, and not complete Alice coverage.' "$SOURCE_ASSET" >/dev/null
      printf '%s\n' 'lesson_brief'
      printf '%s\n' 'student_activity_sheet'
      printf '%s\n' 'proximity_range_frame'
      printf '%s\n' instructor-acceptance-contract-ok
  expect:
    exit_code: 0
    stdout_contains:
    - lesson_brief
    - student_activity_sheet
    - proximity_range_frame
    - instructor-acceptance-contract-ok
  timeout: 60000
assertions:
- name: Assets Validate
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate editable Alice instructor assets
- name: Instructor Acceptance Review Contract Is Runnable
  type: command_success
  agent: eatme-cli-agent
  params:
    step: Validate instructor acceptance review contract
metadata:
  source_eatme_asset: assets/scenarios/eatme/mars-rover-proximity-mission.yaml
  generated_by: eatme-assets gadugi adapter generator
  tags:
  - alice
  - eatme
  - gadugi
  - outside-in
  - instructor
  - agentic
  - mars-rover-proximity-mission
  - mars-rover-proximity-mission
  priority: high
  author: eatme
  test_type: instructor-agentic-flow
