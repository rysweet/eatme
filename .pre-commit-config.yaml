repos:
  - repo: local
    hooks:
      - id: quality-gates
        name: repository quality gates
        entry: env TMPDIR=/tmp ./scripts/quality-gates.sh
        language: system
        pass_filenames: false
